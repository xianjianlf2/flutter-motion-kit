{
  "name": "flutter-motion-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Previewable Flutter animations, each with its pitfalls — reuse straight from Claude Code / Cursor (remote MCP).",
  "workspaces": ["mcp", "site", "worker"],
  "scripts": {
    "catalog": "node scripts/build-catalog.mjs",
    "previews": "node scripts/build-previews.mjs",
    "sync-gists": "node scripts/sync-gists.mjs",
    "mcp:build": "npm -w mcp run build",
    "mcp:dev": "npm -w mcp run dev",
    "site:dev": "npm -w site run dev",
    "site:build": "npm run catalog && npm -w site run build",
    "site:deploy": "npm run site:build && wrangler pages deploy site/dist --project-name flutter-motion-kit",
    "worker:dev": "npm -w worker run dev",
    "worker:deploy": "npm -w worker run deploy"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.5.0"
  }
}
